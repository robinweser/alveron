{
  "pipeline": {
    "build": {
      "outputs": [".next/**"]
    },
    "alveron#build": {
      "outputs": ["lib/**", "es/**"]
    },
    "alveron-middleware-logger#build": {
      "outputs": ["lib/**", "es/**"]
    },
    "alveron-middleware-persistance#build": {
      "outputs": ["lib/**", "es/**"]
    }
  }
}
