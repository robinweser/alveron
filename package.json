{
  "name": "alveron",
  "private": true,
  "scripts": {
    "build": "pnpm turbo run build",
    "release": "pnpm prepare-release && lerna publish --force-publish",
    "prepare-release": "git pull --rebase && pnpm build"
  },
  "workspaces": [
    "packages/*",
    "website"
  ],
  "packageManager": "pnpm@7.11.0",
  "devDependencies": {
    "prettier": "^2.7.1",
    "rimraf": "^3.0.2",
    "turbo": "^1.5.6"
  }
}
