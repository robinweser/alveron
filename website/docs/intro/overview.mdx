# Overview

Alveron is an Elm-inspired state management library for React that supports asynchronous side effects as a first class citizen.<br />
It's super lightweight at only 1.2kb and uses React's useState under the hood.
